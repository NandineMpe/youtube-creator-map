import { schemaVersion } from "@creator-map/shared-schemas";

export default function Home() {
  return (
    <main>
      <h1>YouTube Creator Training Data Map</h1>
      <p>Public application scaffold (schema {schemaVersion.value}).</p>
    </main>
  );
}
